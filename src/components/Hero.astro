---
const roles = ['Product Manager', 'Software Engineer', 'AI Specialist'];
const stats = [
  { num: '5+', label: 'Anos exp.' },
  { num: '30+', label: 'Projetos' },
  { num: '100%', label: 'Resultados' },
];
---
<section id="hero" class="w-full px-5 md:px-16 pt-10 md:pt-20 pb-12 md:pb-24 bg-background">
  <!-- Mobile layout -->
  <div class="md:hidden flex flex-col gap-6">
    <div class="flex items-center gap-2" data-hero-eyebrow>
      <span class="w-2 h-2 rounded-full inline-block" style="background-color: #22c55e;"></span>
      <span class="text-xs text-muted-foreground font-medium">Disponível para novos projetos</span>
    </div>
    <div class="flex items-start justify-between gap-4" data-hero-title-row>
      <div>
        <h1 class="font-headings font-bold leading-none text-foreground" style="font-size: 48px; letter-spacing: -2px; line-height: 1;">
          Rinel<br />
          <span class="italic font-light">Benjamim</span>
        </h1>
      </div>
      <div class="w-28 h-28 rounded-xl overflow-hidden flex-shrink-0 relative" data-hero-photo>
        <img src="/images/hero-photo.png" alt="Rinel Benjamim" class="w-full h-full object-cover object-top" />
      </div>
    </div>
    <div class="flex flex-wrap gap-2" data-hero-subtitle>
      {roles.map((tag) => (
        <span class="text-xs font-medium border border-border rounded-xl px-3 py-1.5 text-muted-foreground bg-muted">{tag}</span>
      ))}
    </div>
    <p class="text-sm text-muted-foreground leading-relaxed" data-hero-desc>
      Construo tecnologia que resolve problemas reais. Especialista em sistemas ERP, plataformas digitais e agentes de IA — do código à estratégia de produto.
    </p>
    <div class="flex gap-3" data-hero-buttons>
      <a href="#projects" class="flex-1 bg-primary text-primary-foreground text-sm font-medium py-3 rounded-md text-center">Ver Projetos</a>
      <a href="#" class="flex-1 border border-border text-foreground text-sm font-medium py-3 rounded-md text-center">Baixar CV</a>
    </div>
    <div class="flex gap-6 pt-4 border-t border-border" data-hero-stats>
      {stats.map((s) => (
        <div class="flex flex-col">
          <span class="font-headings font-bold text-2xl text-foreground">{s.num}</span>
          <span class="text-xs text-muted-foreground">{s.label}</span>
        </div>
      ))}
    </div>
  </div>

  <!-- Desktop layout -->
  <div class="hidden md:flex flex-row md:items-start md:justify-between md:gap-16">
    <div class="flex flex-col gap-8 max-w-2xl pt-4">
      <div class="flex items-center gap-2" data-hero-eyebrow>
        <span class="w-2 h-2 rounded-full inline-block" style="background-color: #22c55e;"></span>
        <span class="text-sm text-muted-foreground font-medium">Disponível para novos projetos</span>
      </div>
      <div data-hero-title>
        <h1 class="font-headings font-bold leading-none text-foreground" style="font-size: clamp(40px, 8vw, 72px); letter-spacing: -3px; line-height: 1;">
          Rinel<br />
          <span class="italic font-light">Benjamim</span>
        </h1>
      </div>
      <div class="flex flex-wrap gap-2" data-hero-subtitle>
        {roles.map((tag) => (
          <span class="text-xs font-medium border border-border rounded-xl px-3 py-1.5 text-muted-foreground bg-muted">{tag}</span>
        ))}
      </div>
      <p class="text-base text-muted-foreground leading-relaxed max-w-lg" data-hero-desc>
        Construo tecnologia que resolve problemas reais. Especialista em sistemas ERP, plataformas digitais e agentes de IA — do código à estratégia de produto.
      </p>
      <div class="flex gap-4" data-hero-buttons>
        <a href="#projects" class="bg-primary text-primary-foreground text-sm font-medium px-6 py-3 rounded-md text-center hover:opacity-90 transition-opacity">Ver Projetos</a>
        <a href="#" class="border border-border text-foreground text-sm font-medium px-6 py-3 rounded-md text-center bg-card hover:bg-muted transition-colors">Baixar CV</a>
      </div>
      <div class="flex gap-8 pt-2" data-hero-stats>
        {stats.map((s) => (
          <div class="flex flex-col">
            <span class="font-headings font-bold text-2xl text-foreground">{s.num}</span>
            <span class="text-xs text-muted-foreground">{s.label}</span>
          </div>
        ))}
      </div>
    </div>

    <div class="relative flex-shrink-0" data-hero-photo>
      <div class="w-96 h-96 rounded-xl overflow-hidden bg-muted relative">
        <img src="/images/hero-photo.png" alt="Rinel Benjamim" class="w-full h-full object-cover object-top" />
        <div class="absolute bottom-4 left-4 bg-card border border-border rounded-lg px-4 py-3 flex items-center gap-3">
          <div class="w-9 h-9 rounded-full flex items-center justify-center" style="background-color: var(--color-accent);">
            <img src="/images/code-xml.svg" alt="" class="w-4 h-4" />
          </div>
          <div>
            <p class="text-xs font-semibold text-foreground">Full Stack</p>
            <p class="text-xs text-muted-foreground">PM + DEV + AI</p>
          </div>
        </div>
      </div>
      <div class="absolute -top-6 -right-6 grid grid-cols-5 gap-1.5 opacity-30">
        {Array.from({length: 25}).map(() => (
          <div class="w-1.5 h-1.5 rounded-full bg-foreground"></div>
        ))}
      </div>
    </div>
  </div>
</section>
